'use client'; import {useState} from 'react'; const API=process.env.NEXT_PUBLIC_API_URL||'http://localhost:4000';
export default function NewProject(){const [busy,setBusy]=useState(false);async function submit(e){e.preventDefault();setBusy(true);const f=new FormData(e.currentTarget);const r=await fetch(`${API}/projects`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({name:f.get('name'),description:f.get('description'),requirements:f.get('requirements')})});const d=await r.json(); if(d.project) location.href=`/projects/${d.project.id}`; else alert(d.error||'failed');setBusy(false)}return <main className="wrap"><a href="/" className="muted">← Projects</a><h1>New Project</h1><form className="form" onSubmit={submit}><input className="input" name="name" placeholder="Project name" required/><input className="input" name="description" placeholder="Short description"/><textarea className="area" name="requirements" placeholder="Describe requirements..." required/><button className="btn" disabled={busy}>{busy?'Starting...':'Create & Start'}</button></form></main>}
