import ChangeForm from '../../components/ChangeForm';
const API=process.env.API_INTERNAL_URL||'http://api:4000'; export default async function Project({params}){const {id}=await params;const p=await (await fetch(`${API}/projects/${id}`,{cache:'no-store'})).json();return <main className="wrap"><a href="/" className="muted">← Projects</a><div className="top"><div><h1>{p.name}</h1><div className="muted">{p.requirements}</div></div></div><h2>Runs</h2><div className="grid">{p.runs?.map(r=><a className="card" key={r.id} href={`/projects/${p.id}/runs/${r.id}`}><b>{r.type}</b><p>{r.request}</p><span className="status">{r.status} · {r.current_stage}</span></a>)}</div><h2>New change</h2><ChangeForm projectId={p.id}/></main>}
